apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: unleash

resources:
  - namespace.yaml
  - postgres.yaml
  - unleash-server.yaml
  - ingress.yaml
  - networkpolicy.yaml
  # Admin must seal + commit these (see secrets.README.md). The
  # Application will stay OutOfSync until both are present.
  # - unleash-postgres-credentials.sealed.yaml
  # - unleash-admin-credentials.sealed.yaml
