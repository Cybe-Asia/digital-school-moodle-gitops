apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moodle
  namespace: skillz-test
spec:
  rules:
  - host: skillz-test.cybe.tech
    http:
      paths:
      - backend:
          service:
            name: moodle
            port:
              number: 8080
        path: /
        pathType: Prefix
