apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: school-test

resources:
- namespace.yaml
- ../../base/admission
- ../../base/auth
- ../../base/notification
- ../../base/otp
- ../../base/payment
- ../../base/frontend
- ../../base/neo4j
- ../../base/minio
- ../../base/governance
- ingress.yaml
- resource-quota.yaml
- app-config.yaml
- app-secrets-sealed.yaml
- neo4j-secret-sealed.yaml
- minio-secret-sealed.yaml
- unleash-frontend-token.sealed.yaml

    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
patches:
- patch: |-
    - op: replace
      path: /spec/type
      value: NodePort
    - op: add
      path: /spec/ports/0/nodePort
      value: 31100
  target:
    kind: Service
    name: admission-service
- patch: |-
    - op: replace
      path: /spec/type
      value: NodePort
    - op: add
      path: /spec/ports/0/nodePort
      value: 31103
  target:
    kind: Service
    name: auth-service
- patch: |-
    - op: replace
      path: /spec/type
      value: NodePort
    - op: add
      path: /spec/ports/0/nodePort
      value: 31102
  target:
    kind: Service
    name: notification-service
- patch: |-
    - op: replace
      path: /spec/type
      value: NodePort
    - op: add
      path: /spec/ports/0/nodePort
      value: 31104
  target:
    kind: Service
    name: otp-service
- patch: |-
    - op: replace
      path: /spec/type
      value: NodePort
    - op: add
      path: /spec/ports/0/nodePort
      value: 31105
  target:
    kind: Service
    name: frontend
- patch: |-
    - op: replace
      path: /spec/type
      value: NodePort
    - op: add
      path: /spec/ports/0/nodePort
      value: 31106
  target:
    kind: Service
    name: payment-service
- patch: |-
    # Resolve school-test.cybe.tech → traefik ClusterIP inside the pod so
    # the Next.js proxy sends Host: school-test.cybe.tech → traefik's ingress
    # rule for school-test-ingress matches → requests stay in this namespace.
    # (Without this the Host would be traefik.kube-system.* and traefik would
    # pick the wildcard school-dev-ingress and route to the wrong namespace.)
    - op: add
      path: /spec/template/spec/hostAliases
      value:
        - ip: "10.43.115.109"
          hostnames: ["school-test.cybe.tech"]
    - op: add
      path: /spec/template/spec/containers/0/env/-
      value:
        name: API_GATEWAY_URL
        value: "http://school-test.cybe.tech"
    - op: add
      path: /spec/template/spec/containers/0/env/-
      value:
        name: NEXT_PUBLIC_API_GATEWAY_URL
        value: "http://school-test.cybe.tech"
    # Feature flags (Unleash). UNLEASH_FRONTEND_TOKEN is pulled from
    # the sealed secret `unleash-frontend-token` — admin must seal the
    # token (scoped to environment=test) before the frontend pod can
    # start with flags enabled. Without the secret, frontend still
    # boots and runs in "disabled" mode, using code defaults.
    - op: add
      path: /spec/template/spec/containers/0/env/-
      value:
        name: UNLEASH_URL
        value: "https://unleash.cybe.tech:8443/api/frontend"
    - op: add
      path: /spec/template/spec/containers/0/env/-
      value:
        name: UNLEASH_ENVIRONMENT
        # Unleash OSS supports only built-in environments (development,
        # production). All lab envs (dev/test/staging) map to Unleash's
        # "development" — same flag state in Unleash flips buttons across
        # the entire lab cluster. Prod uses "production" so it's isolated.
        value: "development"
    - op: add
      path: /spec/template/spec/containers/0/env/-
      value:
        name: UNLEASH_FRONTEND_TOKEN
        valueFrom:
          secretKeyRef:
            name: unleash-frontend-token
            key: token
            optional: true
  target:
    kind: Deployment
    name: frontend
images:
- name: ghcr.io/ariefnugrahaaa/payment-services
  newName: ghcr.io/ariefnugrahaaa/payment-services
  newTag: sha-446071f
- name: ghcr.io/cybe-asia/admission-services
  newName: ghcr.io/cybe-asia/admission-services
  newTag: sha-2da06e9
- name: ghcr.io/cybe-asia/auth-services
  newName: ghcr.io/cybe-asia/auth-services
  newTag: sha-c70a7fa
- name: ghcr.io/cybe-asia/digital-school-frontend
  newName: ghcr.io/cybe-asia/digital-school-frontend
  newTag: sha-71f5cbe
- name: ghcr.io/cybe-asia/notification-services
  newName: ghcr.io/cybe-asia/notification-services
  newTag: sha-59bbf0a
- name: ghcr.io/cybe-asia/otp-services
  newName: ghcr.io/cybe-asia/otp-services
  newTag: sha-bf8101b
