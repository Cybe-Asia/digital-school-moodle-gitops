apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - live-ingress.yaml
  # Moodle routing — see live-moodle-service.yaml for the blue/green
  # cutover notes (Moodle DB must be migrated between colors manually).
  - live-moodle-service.yaml
  - moodle-live-ingress.yaml
